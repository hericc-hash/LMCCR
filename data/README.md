# Data directory

- `raw/`: locally authorized, de-identified source data. Never committed.
- `derived/`: feature banks, mediation tensors, handoffs, and frozen selection manifests. Never committed.

See `docs/DATA_SETUP.md` for the required tensor contracts and filenames.
